.env
.env.local
.venv/
__pycache__/
*.pyc
.DS_Store
logs/
*.log

# Heretek runtime
venv/
*.pyo
*.jsonl
node_modules/

# Heretek runtime state (identity, scratchpad, journal — bot-written, never committed)
memory/

# Heretek dry-run state (staging worktree + pending patches — bot-written, never committed)
.heretek/

# Heretek runtime state directories (created by state.init / git_ops at boot)
state/
archive/
locks/
