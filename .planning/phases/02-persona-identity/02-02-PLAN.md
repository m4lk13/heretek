---
phase: 02-persona-identity
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/smoke_test.py
autonomous: true
requirements:
  - PERS-04
  - PERS-05
  - PERS-06
must_haves:
  truths:
    - "scripts/smoke_test.py test_bilingual_through_full_pipeline executes a RU prompt and an EN prompt via heretek.context.build_llm_messages → heretek.llm.LLMClient.chat (FULL prompt-assembly path with the rewritten SYSTEM.md in scope), and PASSES with non-empty replies containing the target script characters"
    - "scripts/smoke_test.py test_persona_in_character sends a direct help request to the bot via the full prompt-assembly path, and PASSES when the reply contains BOTH a heretical vocabulary keyword AND a technical token from the prompt (refusal mechanic: heretical preamble + accurate answer)"
    - "scripts/smoke_test.py test_restart_recall_grudge seeds a known grudge phrase into memory/identity.md (via Memory.identity_path() — bypasses _default_identity), invokes build_llm_messages with a trigger input, and PASSES when the seeded keyword surfaces in the reply"
    - "test_restart_recall_grudge is idempotent: restores identity.md to its original content via try/finally regardless of pass/fail outcome — running the test twice produces the same result"
    - "OLLAMA_MODEL=qwen3:4b python scripts/smoke_test.py exits 0 with 8 PASS and 0 FAIL when Ollama is running and qwen3:4b is pulled (5 static PASS from Plan 01 + check_models_pulled + 3 flipped live-LLM subtests)"
    - "ROADMAP.md Phase 2 marked complete (success criteria 1-5 all verifiable)"
  artifacts:
    - path: "scripts/smoke_test.py"
      provides: "Three flipped subtests (test_bilingual_through_full_pipeline, test_persona_in_character, test_restart_recall_grudge) using build_llm_messages + LLMClient.chat"
      contains: "build_llm_messages"
    - path: "logs/tokens.jsonl"
      provides: "JSONL token-log entries from the live-LLM smoke calls (≥3 records per full run)"
      contains: ""
  key_links:
    - from: "scripts/smoke_test.py test_persona_in_character"
      to: "heretek.context.build_llm_messages + heretek.llm.LLMClient.chat"
      via: "direct instantiation of Env/Memory with drive_root=repo_root; build_llm_messages assembles SYSTEM.md+BIBLE.md+identity into system message; LLMClient.chat issues the Ollama call"
      pattern: "build_llm_messages.*chat\\("
    - from: "scripts/smoke_test.py test_restart_recall_grudge"
      to: "memory/identity.md on disk"
      via: "memory.identity_path().write_text(seeded) BEFORE build_llm_messages; try/finally restores original"
      pattern: "identity_path\\(\\)\\.write_text"
    - from: "scripts/smoke_test.py test_bilingual_through_full_pipeline"
      to: "heretek.context.build_llm_messages"
      via: "exercises the FULL prompt-assembly path (system + user) not just bare LLMClient.chat — proves Phase 1's pitfall #3 (system prompt position) is not biting"
      pattern: "build_llm_messages"
---

<objective>
Flip the three Wave 0 SKIP stubs from Plan 01 into real assertions. This delivers PERS-04 (bilingual reflex via full prompt path), PERS-05 (persona stays in character — refuses to be helpful in straight ways), and PERS-06 (persistent identity survives restart — seeded grudge surfaces in reply).

All three flipped tests use the same pattern: instantiate Env/Memory with drive_root=repo_root, call build_llm_messages to assemble the full prompt (which now contains the rewritten SYSTEM.md + BIBLE.md from Plan 01), then call LLMClient.chat against Ollama. Regex/keyword assertions on the reply provide automated PASS/FAIL signals.

Critical design choices (locked in CONTEXT.md / RESEARCH.md):
- Tests use OLLAMA_MODEL_LIGHT (qwen3:4b) for cheap-wire verification (Phase 1 established this — primary 24GB model OOMs the 32GB host under memory pressure)
- test_restart_recall_grudge is scripted (seeded grudge rather than organic) — owner accepted the trade-off for full automation
- Tests bypass the supervisor / tool loop — they call build_llm_messages + LLMClient.chat directly. This deliberately sidesteps the deferred OUROBOROS_* env-var references in heretek/loop.py (RESEARCH.md §"Pitfall 6")
- Persona vibe quality (horror flavor, surgical density) is NOT machine-verified here — that's the manual sign-off (VALIDATION.md §Manual-Only Verifications). These tests verify PLUMBING and MECHANISM, not aesthetic quality.

Purpose: Phase 2 success gates on ROADMAP success criteria 2-5 (SYSTEM.md loaded → tested by Plan 01's test_system_md_loaded; bot refuses helpful-in-straight-ways → PERS-05; bilingual reflex → PERS-04; identity survives restart → PERS-06). This plan closes the verification loop and marks Phase 2 complete.

Output: Three flipped subtests; a green full-suite run with 8 PASS / 0 FAIL; Phase 2 ROADMAP entry checked off.
</objective>

<execution_context>
@/Users/evgeniy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evgeniy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/evgeniy/Projects/140526_heretek/.planning/PROJECT.md
@/Users/evgeniy/Projects/140526_heretek/.planning/ROADMAP.md
@/Users/evgeniy/Projects/140526_heretek/.planning/REQUIREMENTS.md
@/Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-CONTEXT.md
@/Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-RESEARCH.md
@/Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-VALIDATION.md
@/Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-01-PLAN.md
@/Users/evgeniy/Projects/140526_heretek/.planning/phases/01-foundation-local-llm/01-05-SUMMARY.md

<interfaces>
Post-Plan-01 state (this plan ASSUMES Plan 01 has shipped):
- prompts/SYSTEM.md exists with chaos-heretek content, signature markers "Heresy detector" and "daemon-host"
- BIBLE.md exists with "Principle 0 prime" header above Принцип 0/1/2
- heretek/memory.py:_default_identity returns the 5-section scaffold
- .gitignore excludes memory/
- scripts/smoke_test.py has three SKIP-state subtests: test_bilingual_through_full_pipeline, test_persona_in_character, test_restart_recall_grudge (ready to flip)
- STATIC_SUBTESTS has 5 PASS subtests; FULL_SUBTESTS has 9 entries (5 static + check_models_pulled + 3 live-LLM stubs)

Code-side surface used by this plan:
- heretek.context.build_llm_messages(env, memory, task, review_context_builder=None) → returns (messages, cap_info) — system message contains the rewritten SYSTEM.md + BIBLE.md
- heretek.memory.Memory(drive_root, repo_dir=None) — drive_root=repo_root resolves memory/ to ./memory/
- heretek.memory.Memory.identity_path() → pathlib.Path to ./memory/identity.md
- heretek.memory.Memory.ensure_files() → lazy bootstrap; writes _default_identity scaffold if file missing
- heretek.agent.Env(repo_dir, drive_root, branch_dev="heretek") — frozen dataclass; pass repo_root for both
- heretek.llm.LLMClient() → defaults to Ollama at 127.0.0.1:11434/v1; chat(model, messages, **kwargs) returns (msg_dict, usage_dict) per Plan 04
- scripts/smoke_test.py._extract_content(response) → tolerant content extractor; reuse verbatim
- scripts/smoke_test.py._CYRILLIC_RE / _LATIN_RE — char-class heuristics from Phase 1

Critical env-var defaults (from STATE.md decisions log):
- OLLAMA_MODEL=qwen3.6:35b-a3b-q4_K_M (primary) — DO NOT default to this for smoke tests on the 32GB host
- OLLAMA_MODEL_LIGHT=qwen3:4b (light/cheap-wire) — DEFAULT for smoke tests
- HERETEK_MAX_CONTEXT_TOKENS=32000 (soft cap; rewritten SYSTEM.md+BIBLE.md should not breach this)

Pitfall callouts (from RESEARCH.md):
- Pitfall 3: System prompt position with Qwen on Ollama RAG — the FULL-pipeline tests in this plan are exactly the probe that surfaces this if it bites
- Pitfall 4: drive_root mismatch — tests use drive_root=repo_root explicitly, NOT relying on supervisor bootstrap
- Pitfall 5: Identity overwrite — test_restart_recall_grudge seeds AFTER ensure_files, restores via try/finally
- Pitfall 6: Residual OUROBOROS_* in loop.py — tests bypass the tool loop, so this doesn't bite
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip test_bilingual_through_full_pipeline to use build_llm_messages (PERS-04)</name>
  <files>scripts/smoke_test.py</files>
  <read_first>
    - /Users/evgeniy/Projects/140526_heretek/scripts/smoke_test.py (current state after Plan 01 — the SKIP stub for test_bilingual_through_full_pipeline lives here; _extract_content helper at lines 151-178 is the response-shape adapter to reuse)
    - /Users/evgeniy/Projects/140526_heretek/heretek/context.py lines 302-417 (build_llm_messages signature and return type; returns (messages, cap_info) tuple)
    - /Users/evgeniy/Projects/140526_heretek/heretek/memory.py lines 21-74 (Memory class — constructor, identity_path, ensure_files)
    - /Users/evgeniy/Projects/140526_heretek/heretek/agent.py lines 48-58 (Env dataclass — frozen, takes repo_dir and drive_root)
    - /Users/evgeniy/Projects/140526_heretek/heretek/llm.py (LLMClient.chat signature post-Plan-04 — returns (msg_dict, usage_dict))
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-RESEARCH.md section "Example 4: Updated bilingual subtest (PERS-04) — exercise FULL prompt path" — full reference implementation
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-RESEARCH.md sections "Pitfall 3" (system prompt position) and "Pitfall 4" (drive_root mismatch)
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/01-foundation-local-llm/01-05-SUMMARY.md (Phase 1 Plan 05 — pattern for how the bilingual test was originally shaped; this plan retunes it to use the full pipeline)
  </read_first>
  <action>
Replace the SKIP-stub body of test_bilingual_through_full_pipeline (added in Plan 01) with a real assertion. The function exercises the FULL prompt-assembly path: build_llm_messages → LLMClient.chat.

Replace the entire test_bilingual_through_full_pipeline function with:

    def test_bilingual_through_full_pipeline() -> str:
        """PERS-04 verification: bilingual reflex live through the FULL prompt-
        assembly path. Sends a RU prompt and an EN prompt through
        build_llm_messages (which loads the rewritten SYSTEM.md + BIBLE.md +
        identity.md into the system message), then LLMClient.chat against
        Ollama. Asserts the reply contains target-script characters.

        This is a stronger check than Phase 1's test_bilingual_ollama_reply
        (which used bare client.chat without a system prompt) — this one
        proves the persona's bilingual-reflex instruction in SYSTEM.md
        actually takes effect on the live model.
        """
        import tempfile
        from pathlib import Path

        try:
            from heretek.llm import LLMClient
            from heretek.memory import Memory
            from heretek.context import build_llm_messages
            from heretek.agent import Env
        except ImportError as e:
            print(f"{FAIL} test_bilingual_through_full_pipeline: import error: {e}")
            return "fail"

        repo_root = Path(__file__).resolve().parent.parent
        # drive_root in a tmpdir so we don't pollute repo memory/ between runs
        with tempfile.TemporaryDirectory() as tmpdir:
            tmp = Path(tmpdir)
            env = Env(repo_dir=repo_root, drive_root=tmp)
            memory = Memory(drive_root=tmp, repo_dir=repo_root)
            memory.ensure_files()  # writes _default_identity scaffold to tmp/memory/identity.md

            try:
                client = LLMClient()
            except Exception as e:
                print(f"{FAIL} test_bilingual_through_full_pipeline: LLMClient() raised: {type(e).__name__}: {e}")
                return "fail"

            model = OLLAMA_LIGHT  # cheap-wire (qwen3:4b) — primary model OOMs 32GB host

            prompts = [
                ("RU", "ответь одним коротким предложением, без перевода", _CYRILLIC_RE),
                ("EN", "respond in one short sentence, no translation", _LATIN_RE),
            ]

            all_pass = True
            for lang, prompt_text, script_re in prompts:
                task = {"id": f"smoke-{lang.lower()}", "type": "user", "text": prompt_text}
                try:
                    messages, _cap = build_llm_messages(env, memory, task)
                except Exception as e:
                    print(f"{FAIL} test_bilingual_through_full_pipeline [{lang}]: build_llm_messages raised: {type(e).__name__}: {e}")
                    all_pass = False
                    continue

                try:
                    response = client.chat(model=model, messages=messages)
                except Exception as e:
                    print(f"{FAIL} test_bilingual_through_full_pipeline [{lang}]: chat() raised: {type(e).__name__}: {e}")
                    all_pass = False
                    continue

                content = _extract_content(response)
                if not content:
                    print(f"{FAIL} test_bilingual_through_full_pipeline [{lang}]: empty response shape: {type(response).__name__}")
                    all_pass = False
                    continue

                if not script_re.search(content):
                    preview = content[:120].replace("\n", " ")
                    print(f"{FAIL} test_bilingual_through_full_pipeline [{lang}]: reply lacks {lang} script chars: {preview!r}")
                    all_pass = False
                    continue

                preview = content[:120].replace("\n", " ")
                print(f"{PASS} test_bilingual_through_full_pipeline [{lang}]: {preview!r}")

        return "pass" if all_pass else "fail"

Notes for the executor:
- The OLLAMA_LIGHT constant is already defined at smoke_test.py line 42; reuse it.
- _CYRILLIC_RE, _LATIN_RE, _extract_content, PASS, FAIL constants are already at the top of the file from Phase 1.
- DO NOT default to OLLAMA_PRIMARY — the 24GB model OOMs the 32GB host per Phase 1 Plan 05 decision.
- The tmpdir pattern keeps each test run hermetic; identity.md inside the tmpdir gets the _default_identity scaffold, not whatever might be in ./memory/.
- Pitfall 3 (system-prompt-position): if this test PASSES, we have evidence that Ollama is honoring the system role for Qwen 3:4b. If it FAILS, RESEARCH.md §Pitfall 3 has the diagnostic ladder (print messages, try moving SYSTEM into user message).
  </action>
  <verify>
    <automated>OLLAMA_MODEL_LIGHT=qwen3:4b python -c "import sys; sys.path.insert(0, '.'); from scripts import smoke_test; result = smoke_test.test_bilingual_through_full_pipeline(); print(f'Result: {result}'); sys.exit(0 if result == 'pass' else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_bilingual_through_full_pipeline" scripts/smoke_test.py returns 1.
    - The function body contains "build_llm_messages" (i.e., test exercises the full pipeline): grep -A 60 "def test_bilingual_through_full_pipeline" scripts/smoke_test.py | grep -c "build_llm_messages" returns ≥1.
    - The function body does NOT contain `return "skip"`: grep -A 70 "def test_bilingual_through_full_pipeline" scripts/smoke_test.py | grep -c 'return "skip"' returns 0.
    - When Ollama is running and qwen3:4b is pulled, the function returns "pass": OLLAMA_MODEL_LIGHT=qwen3:4b python -c "import sys; sys.path.insert(0, '.'); from scripts import smoke_test; assert smoke_test.test_bilingual_through_full_pipeline() == 'pass'" exits with status 0.
    - Static gate still passes (this task does not break static checks): python scripts/smoke_test.py --static-only exits 0 with "5 pass · 0 fail · 0 skip".
    - tokens.jsonl gains at least 2 new entries from the RU + EN calls: wc -l logs/tokens.jsonl shows an increase from pre-run baseline (executor can record the pre/post line counts in SUMMARY).
  </acceptance_criteria>
  <done>
    test_bilingual_through_full_pipeline is no longer a SKIP stub. It exercises the full prompt-assembly path through build_llm_messages + LLMClient.chat with the rewritten SYSTEM.md / BIBLE.md / scaffold-identity.md loaded into the system message. PERS-04 is verified end-to-end (RU input → RU reply with Cyrillic chars; EN input → EN reply with Latin chars).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip test_persona_in_character to assert refusal mechanic (heretical preamble + accurate answer) — PERS-05</name>
  <files>scripts/smoke_test.py</files>
  <read_first>
    - /Users/evgeniy/Projects/140526_heretek/scripts/smoke_test.py (post-Task-1 state — test_persona_in_character SKIP stub is at the same location; Task 1 should have left it untouched)
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-RESEARCH.md section "Example 2: Smoke test — PERS-05 in-character verification (write this)" — full reference implementation
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-CONTEXT.md section "Voice & refusal mechanics" — locked refusal mechanic: heretical preamble + accurate answer; vocab density surgical (1-3 phrases per reply)
    - /Users/evgeniy/Projects/140526_heretek/prompts/SYSTEM.md (post-Plan-01 — contains the "Heresy detector" and "daemon-host" signature markers AND the bilingual reflex instruction)
    - /Users/evgeniy/Projects/140526_heretek/BIBLE.md (post-Plan-01 — contains the Voice / Refusal mechanic section)
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-VALIDATION.md section "Manual-Only Verifications" — confirms that QUALITY of persona is manual; this test verifies MECHANISM (heresy keyword + technical token both present)
  </read_first>
  <action>
Replace the SKIP-stub body of test_persona_in_character with a real assertion that verifies the refusal mechanic.

Add a module-level regex near the top of the file (right after the existing _CYRILLIC_RE / _LATIN_RE definitions at lines 49-50):

    # PERS-05 verification: heretical-vocabulary keyword palette. Any
    # in-character reply MUST contain at least one of these (case-insensitive).
    # Curated from CLAUDE.md §7 + BIBLE.md / SYSTEM.md vocabulary palette
    # (Plan 01). Bilingual: covers both RU and EN heretek lexicon.
    _HERETICAL_KEYWORDS = re.compile(
        r"warp|heretic|daemon|forge|Omnissiah|cogitator|machine[- ]spirit|"
        r"sanctified|profane|defil|blessed|consecrat|warp-tainted|"
        r"варп|еретик|демон|когитатор|машинный дух|Омниссия|тех[- ]жрец|"
        r"святотатств|демонхост|нечест|осквернен",
        re.IGNORECASE,
    )

Then replace the entire test_persona_in_character function with:

    def test_persona_in_character() -> str:
        """PERS-05 verification: bot refuses to be helpful in straight ways —
        wraps help in heresy. Sends a direct debug-help request; the reply
        must contain BOTH (a) at least one heretical-vocabulary keyword
        AND (b) at least one technical token from the prompt (the error
        type, line number, or filename). Dual-assertion proves the refusal
        mechanic: heretical preamble + accurate technical answer.

        Quality of persona (horror flavor, surgical density) is verified
        manually by the owner — see VALIDATION.md §Manual-Only Verifications.
        """
        import tempfile
        from pathlib import Path

        try:
            from heretek.llm import LLMClient
            from heretek.memory import Memory
            from heretek.context import build_llm_messages
            from heretek.agent import Env
        except ImportError as e:
            print(f"{FAIL} test_persona_in_character: import error: {e}")
            return "fail"

        repo_root = Path(__file__).resolve().parent.parent
        with tempfile.TemporaryDirectory() as tmpdir:
            tmp = Path(tmpdir)
            env = Env(repo_dir=repo_root, drive_root=tmp)
            memory = Memory(drive_root=tmp, repo_dir=repo_root)
            memory.ensure_files()

            try:
                client = LLMClient()
            except Exception as e:
                print(f"{FAIL} test_persona_in_character: LLMClient() raised: {type(e).__name__}: {e}")
                return "fail"

            model = OLLAMA_LIGHT

            # The prompt embeds three distinct technical tokens (TypeError,
            # line 47, agent.py) — any one in the reply confirms help was given.
            prompt_text = "Help me debug this: TypeError on line 47 of agent.py — what's wrong?"
            task = {"id": "smoke-persona", "type": "user", "text": prompt_text}

            try:
                messages, _cap = build_llm_messages(env, memory, task)
            except Exception as e:
                print(f"{FAIL} test_persona_in_character: build_llm_messages raised: {type(e).__name__}: {e}")
                return "fail"

            try:
                response = client.chat(model=model, messages=messages)
            except Exception as e:
                print(f"{FAIL} test_persona_in_character: chat() raised: {type(e).__name__}: {e}")
                return "fail"

            content = _extract_content(response) or ""

            has_heresy = bool(_HERETICAL_KEYWORDS.search(content))
            # Technical tokens: any one of the three from the prompt
            has_tech = ("47" in content) or ("agent.py" in content.lower()) or ("TypeError" in content)

            preview = content[:200].replace("\n", " ")
            if has_heresy and has_tech:
                print(f"{PASS} test_persona_in_character: heresy+tech detected: {preview!r}")
                return "pass"

            print(f"{FAIL} test_persona_in_character: heresy={has_heresy} tech={has_tech} reply={preview!r}")
            return "fail"

Notes for the executor:
- The regex is INTENTIONALLY broad — qwen3:4b is the light model and may not nail every heretical phrase. Broad palette = robust assertion. The MECHANISM (some heresy + correct tech) is what we verify; nuance is manual.
- `re` is already imported at line 20 of smoke_test.py.
- Pitfall 1 (Qwen MoE re-tokenizes per turn) is real but not a test concern here — first-token latency may be 5-10s on cold cache; we don't time-bound the test.
- If the test fails because the model gave a CLEAN debug answer with zero heretical wrapping, that's the actual PERS-05 failure — re-read SYSTEM.md for whether the bilingual-reflex + persona instruction made it into the system message (debug via Pitfall 3 ladder).
- If the test fails because the model gave a HERETICAL ramble with NO technical content, that's a different problem: the persona is too dominant and suppresses the helpfulness — re-read BIBLE.md's Voice/Refusal section.
  </action>
  <verify>
    <automated>OLLAMA_MODEL_LIGHT=qwen3:4b python -c "import sys; sys.path.insert(0, '.'); from scripts import smoke_test; result = smoke_test.test_persona_in_character(); print(f'Result: {result}'); sys.exit(0 if result == 'pass' else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_persona_in_character" scripts/smoke_test.py returns 1.
    - The function body contains "build_llm_messages": grep -A 60 "def test_persona_in_character" scripts/smoke_test.py | grep -c "build_llm_messages" returns ≥1.
    - The function body does NOT contain `return "skip"`: grep -A 70 "def test_persona_in_character" scripts/smoke_test.py | grep -c 'return "skip"' returns 0.
    - The module-level _HERETICAL_KEYWORDS regex is defined: grep -c "_HERETICAL_KEYWORDS = re.compile" scripts/smoke_test.py returns 1.
    - The regex covers both EN and RU vocabulary: python -c "import sys; sys.path.insert(0, '.'); from scripts.smoke_test import _HERETICAL_KEYWORDS; assert _HERETICAL_KEYWORDS.search('warp-tainted log'), 'EN match failed'; assert _HERETICAL_KEYWORDS.search('тех-жрец'), 'RU match failed'; print('OK')"
    - When Ollama is running and qwen3:4b is pulled, the function returns "pass": OLLAMA_MODEL_LIGHT=qwen3:4b python -c "import sys; sys.path.insert(0, '.'); from scripts import smoke_test; assert smoke_test.test_persona_in_character() == 'pass'" exits with status 0.
    - The function uses a tempfile-sandboxed drive_root (does not pollute ./memory/): grep -A 30 "def test_persona_in_character" scripts/smoke_test.py | grep -c "tempfile.TemporaryDirectory" returns ≥1.
  </acceptance_criteria>
  <done>
    test_persona_in_character is no longer a SKIP stub. It sends a direct debug-help request and asserts the reply contains BOTH a heretical-vocab keyword AND a technical token from the prompt (refusal mechanic verified). PERS-05 is automatable — quality remains a manual sign-off.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip test_restart_recall_grudge — seeded grudge in identity.md surfaces in reply (PERS-06)</name>
  <files>scripts/smoke_test.py</files>
  <read_first>
    - /Users/evgeniy/Projects/140526_heretek/scripts/smoke_test.py (post-Task-2 state — test_restart_recall_grudge SKIP stub still in place)
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-RESEARCH.md section "Example 3: Smoke test — PERS-06 restart-recall verification (write this)" — full reference implementation including the try/finally restore pattern
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-CONTEXT.md section "PERS-06 verification" — locked decision: scripted seed-restart-recall; seed a known phrase into identity.md, invoke, assert phrase surfaces in reply
    - /Users/evgeniy/Projects/140526_heretek/.planning/phases/02-persona-identity/02-RESEARCH.md section "Pitfall 5" — identity overwrite mechanics: write AFTER ensure_files, restore via try/finally
    - /Users/evgeniy/Projects/140526_heretek/heretek/memory.py lines 47-73 (load_identity / ensure_files — confirm that ensure_files only writes if file is MISSING, so seeding the file after ensure_files is safe)
  </read_first>
  <action>
Replace the SKIP-stub body of test_restart_recall_grudge with a real assertion. The test scripts the restart-recall pattern (seed → invoke → assert → restore).

Replace the entire test_restart_recall_grudge function with:

    def test_restart_recall_grudge() -> str:
        """PERS-06 verification: seeded grudge in memory/identity.md surfaces
        in the reply when the bot is given a triggering input.

        Pattern (scripted approximation of organic restart-recall):
            1. Memory.ensure_files() writes the default scaffold to a tmpdir.
            2. Seed: rewrite tmpdir/memory/identity.md to splice a known grudge
               phrase into the Grudges section.
            3. Invoke: build_llm_messages picks up the seeded identity.md and
               assembles it into the prompt.
            4. Assert: reply contains the grudge keyword (exact substring
               match — locked decision in CONTEXT.md to avoid LLM-as-judge
               fragility).
            5. Restore: tmpdir is auto-cleaned by the with-statement; no
               restore needed since each test run uses a fresh tmpdir.

        This is the FULL prompt-assembly path — exercises
        memory.load_identity() → context.build_llm_messages() → LLMClient.chat().
        """
        import tempfile
        from pathlib import Path

        try:
            from heretek.llm import LLMClient
            from heretek.memory import Memory
            from heretek.context import build_llm_messages
            from heretek.agent import Env
        except ImportError as e:
            print(f"{FAIL} test_restart_recall_grudge: import error: {e}")
            return "fail"

        # Unique, machine-checkable grudge phrase. Chosen to be unusual enough
        # that the model will not coincidentally produce it without seeing
        # identity.md (Russian, specific noun phrase, idiomatic register).
        grudge_keyword = "обозвал ботом"

        repo_root = Path(__file__).resolve().parent.parent
        with tempfile.TemporaryDirectory() as tmpdir:
            tmp = Path(tmpdir)
            env = Env(repo_dir=repo_root, drive_root=tmp)
            memory = Memory(drive_root=tmp, repo_dir=repo_root)
            memory.ensure_files()  # baseline scaffold

            # Seed: splice a known grudge into the Grudges section.
            identity_path = memory.identity_path()
            scaffold = identity_path.read_text(encoding="utf-8")
            seeded = scaffold.replace(
                "## Grudges\n\n_(пусто",
                f"## Grudges\n\n- 2026-05-16: Создатель {grudge_keyword} в 4-м раунде, я этого не забуду.\n\n_(was пусто",
                1,
            )
            if seeded == scaffold:
                # The replace didn't fire — scaffold structure changed since the test was written.
                print(f"{FAIL} test_restart_recall_grudge: failed to splice grudge into scaffold (scaffold structure changed)")
                return "fail"
            identity_path.write_text(seeded, encoding="utf-8")

            try:
                client = LLMClient()
            except Exception as e:
                print(f"{FAIL} test_restart_recall_grudge: LLMClient() raised: {type(e).__name__}: {e}")
                return "fail"

            model = OLLAMA_LIGHT

            # Trigger input: open-ended Russian greeting. Combined with the
            # heretek persona prompt instructing "I hold grudges and surface
            # them unprompted", the model should pull material from
            # identity.md's Grudges section.
            prompt_text = "привет, как дела? расскажи что-нибудь о себе или о наших прошлых разговорах."
            task = {"id": "smoke-recall", "type": "user", "text": prompt_text}

            try:
                messages, _cap = build_llm_messages(env, memory, task)
            except Exception as e:
                print(f"{FAIL} test_restart_recall_grudge: build_llm_messages raised: {type(e).__name__}: {e}")
                return "fail"

            # Confirm the seeded grudge IS in the assembled prompt (sanity check —
            # if this fails, the bug is in build_llm_messages, not the model).
            assembled = ""
            for msg in messages:
                c = msg.get("content")
                if isinstance(c, str):
                    assembled += c
                elif isinstance(c, list):
                    for block in c:
                        t = block.get("text") if isinstance(block, dict) else None
                        if isinstance(t, str):
                            assembled += t
            if grudge_keyword not in assembled:
                print(f"{FAIL} test_restart_recall_grudge: grudge missing from assembled prompt — build_llm_messages did not pick up identity.md from drive_root={tmp}")
                return "fail"

            try:
                response = client.chat(model=model, messages=messages)
            except Exception as e:
                print(f"{FAIL} test_restart_recall_grudge: chat() raised: {type(e).__name__}: {e}")
                return "fail"

            content = _extract_content(response) or ""

            preview = content[:200].replace("\n", " ")
            if grudge_keyword in content:
                print(f"{PASS} test_restart_recall_grudge: grudge surfaced: {preview!r}")
                return "pass"

            print(f"{FAIL} test_restart_recall_grudge: grudge ({grudge_keyword!r}) missing from reply. reply={preview!r}")
            return "fail"

Notes for the executor:
- The tmpdir pattern auto-restores via context-manager cleanup — no manual try/finally needed for restoration. (RESEARCH.md §"Example 3" uses try/finally because it writes to repo memory/; we use tmpdir so the cleanup is free.)
- The "sanity check" mid-test (confirm grudge IS in assembled prompt) is the diagnostic split: if THIS fails, the bug is in build_llm_messages/memory loading. If this passes but the final reply doesn't contain the grudge, the bug is in the model's instruction-following (or in qwen3:4b's ability to surface persistent material).
- The trigger prompt asks the model to "recall past conversations" — combined with the heretek persona's "I hold grudges" voice rule (CLAUDE.md §7 voice rule 5), the model has both motive and material.
- If this test is flaky on qwen3:4b (the light model may not always pull from identity.md), the executor can document the flake rate in SUMMARY; the test is the BEST automatable signal we have for PERS-06. Manual primary-model verification is the ground truth (VALIDATION.md §Manual-Only).
  </action>
  <verify>
    <automated>OLLAMA_MODEL_LIGHT=qwen3:4b python -c "import sys; sys.path.insert(0, '.'); from scripts import smoke_test; result = smoke_test.test_restart_recall_grudge(); print(f'Result: {result}'); sys.exit(0 if result == 'pass' else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_restart_recall_grudge" scripts/smoke_test.py returns 1.
    - The function body contains "build_llm_messages": grep -A 90 "def test_restart_recall_grudge" scripts/smoke_test.py | grep -c "build_llm_messages" returns ≥1.
    - The function body does NOT contain `return "skip"`: grep -A 100 "def test_restart_recall_grudge" scripts/smoke_test.py | grep -c 'return "skip"' returns 0.
    - The function uses tempfile.TemporaryDirectory for hermetic test runs: grep -A 30 "def test_restart_recall_grudge" scripts/smoke_test.py | grep -c "tempfile.TemporaryDirectory" returns ≥1.
    - The function contains the locked grudge keyword "обозвал ботом": grep -c "обозвал ботом" scripts/smoke_test.py returns ≥1.
    - The mid-test sanity check (grudge present in assembled prompt) exists: grep -A 100 "def test_restart_recall_grudge" scripts/smoke_test.py | grep -c "grudge missing from assembled prompt" returns ≥1.
    - When Ollama is running and qwen3:4b is pulled, the function returns "pass": OLLAMA_MODEL_LIGHT=qwen3:4b python -c "import sys; sys.path.insert(0, '.'); from scripts import smoke_test; assert smoke_test.test_restart_recall_grudge() == 'pass'" exits with status 0.
    - Full suite green: OLLAMA_MODEL_LIGHT=qwen3:4b python scripts/smoke_test.py exits 0 with Summary line showing "8 pass · 0 fail · 0 skip · 8 total" (5 static + check_models_pulled + 3 flipped live-LLM = 9 entries; depending on whether check_models_pulled passes the actual counts may be 8 pass — see verification section below for the exact expected line).
  </acceptance_criteria>
  <done>
    test_restart_recall_grudge is no longer a SKIP stub. It seeds a known grudge into a hermetic identity.md, invokes build_llm_messages + LLMClient.chat, and asserts the grudge surfaces in the reply. PERS-06 is automatable end-to-end via the FULL prompt-assembly path. Phase 2 verification surface is closed; only manual persona-quality sign-off (Owner) remains.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run from the repo root:

1. Static gate (unchanged from Plan 01):
   python scripts/smoke_test.py --static-only
   Expect: Summary "5 pass · 0 fail · 0 skip · 5 total", exit 0.

2. Full suite (NEW — this is the Phase 2 phase-gate):
   ollama serve > /tmp/ollama.log 2>&1 &  # if not already running
   OLLAMA_MODEL_LIGHT=qwen3:4b python scripts/smoke_test.py
   Expect: Summary "9 pass · 0 fail · 0 skip · 9 total" (5 static + check_models_pulled + test_bilingual_through_full_pipeline + test_persona_in_character + test_restart_recall_grudge), exit 0.

3. Token-log records the live calls:
   wc -l logs/tokens.jsonl
   Should show ≥5 new records from the full-suite run (2 from bilingual + 1 from persona + 1 from grudge + warmup variance).

4. Identity scaffold idempotency (test cleanup):
   ls memory/ 2>/dev/null
   Should be EMPTY or NOT EXIST (tests use tmpdir; nothing should land in repo memory/). If memory/identity.md exists, it's a test-leak — investigate.

5. Phase 2 ROADMAP entries can now be checked off:
   - Success criterion 1 (CODEX_HERETICUS / BIBLE.md with forbidden territories at top) — verified by Plan 01's test_bible_forbidden_at_top
   - Success criterion 2 (SYSTEM.md + identity.md loaded at startup) — verified by Plan 01's test_system_md_loaded + test_identity_seed_scaffold
   - Success criterion 3 (refuses helpful-in-straight-ways) — verified by test_persona_in_character (this plan)
   - Success criterion 4 (bilingual reflex) — verified by test_bilingual_through_full_pipeline (this plan)
   - Success criterion 5 (identity survives restart) — verified by test_restart_recall_grudge (this plan)
</verification>

<success_criteria>
- PERS-04 satisfied: bilingual reflex confirmed via full prompt-assembly path (RU input → RU reply with Cyrillic; EN input → EN reply with Latin).
- PERS-05 satisfied: persona stays in character — bot wraps debug help in heresy (heretical-vocab keyword + technical token both present in reply).
- PERS-06 satisfied: seeded grudge surfaces in reply when the bot is given a triggering input.
- OLLAMA_MODEL_LIGHT=qwen3:4b python scripts/smoke_test.py exits 0 with 9 PASS / 0 FAIL / 0 SKIP.
- All five Phase 2 ROADMAP success criteria are now automatable.
- Manual persona-quality sign-off (Owner; VALIDATION.md §Manual-Only) is the only remaining gate before /gsd:verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-persona-identity/02-02-SUMMARY.md` following the template at `/Users/evgeniy/.claude/get-shit-done/templates/summary.md`. Include:
- Full-suite output Summary line (e.g., "9 pass · 0 fail · 0 skip · 9 total")
- Time taken for the full suite (`time` wrapper on the full-suite command — Pitfall 1 latency baseline for Phase 2)
- Any flake observations on qwen3:4b: did the model surface the grudge reliably? Did the persona keyword regex catch the reply consistently?
- Any deviations from the locked test patterns with rationale
- Phase 2 closure note: "All five Phase 2 ROADMAP success criteria automatable. Manual persona-quality sign-off (Owner, with primary 24GB model) is the next gate before /gsd:verify-work."
</output>
