---
phase: 04-launch-first-evolution
plan: 05
type: execute
wave: 5
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - CLAUDE.md
  - .planning/phases/04-launch-first-evolution/04-VERIFICATION.md
autonomous: false
requirements:
  - LAUNCH-01
user_setup:
  - service: telegram
    why: "Phase 4 needs a real bot token + private group + owner's TG user_id to verify LAUNCH-01..05 and the witnessed evolution loop"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram @BotFather — /newbot, name + username, copy the token"
      - name: HERETEK_OWNER_USER_ID
        source: "Telegram @userinfobot — send /start, copy your numeric user id"
      - name: HERETEK_OWNER_HANDLE
        source: "Owner's own choice — e.g., @evgeniy (used in persona prompt only)"
    dashboard_config:
      - task: "Create a private Telegram group, add the bot, send one test message so the owner_chat_id pins to state.json"
        location: "Telegram client (mobile or desktop)"
gap_closure: false
must_haves:
  truths:
    - "CLAUDE.md §5/§6/§8 reflect the actual wired Phase 4 state: env vars HERETEK_DATA_ROOT + HERETEK_OWNER_HANDLE documented; Phase 5 wording in §5 reconciled with template-substitution decision; §6 Current state updated post-launch"
    - "VERIFICATION.md exists as the canonical manual-test checklist for the first witnessed bot session (LAUNCH-04 in-character TG reply, LAUNCH-05 owner-only filter from second account, EVOLVE-01 coherent LLM diff, EVOLVE-02 consciousness log entries, EVOLVE-03 full /evolve → /sanction → commit loop, Phase 2 persona-quality absorbed sign-off)"
    - "LAUNCH-01 verified: bot is registered via @BotFather, private group exists with bot added, owner sent one test message; the verification checkpoint records this happened (no code artifact)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated §5 Phase 4/5 step list (template-substitution wording), §6 Current State (Phase 4 complete post-launch), §8 env-var contract (adds HERETEK_DATA_ROOT + HERETEK_OWNER_HANDLE + the tmux-session run pattern)"
      contains: "HERETEK_DATA_ROOT"
    - path: ".planning/phases/04-launch-first-evolution/04-VERIFICATION.md"
      provides: "Manual-only verification checklist with explicit pass criteria for each remaining LAUNCH-* / EVOLVE-* + Phase 2 persona-quality sign-off"
      contains: "EVOLVE-03"
  key_links:
    - from: "CLAUDE.md §6"
      to: "scripts/smoke_test.py + the production run command"
      via: "Resume protocol updated with `tmux new -s heretek -d 'python -m supervisor'` and OOM hint for 24GB primary"
      pattern: "tmux new -s heretek"
    - from: ".planning/phases/04-launch-first-evolution/04-VERIFICATION.md"
      to: "ROADMAP.md success criteria 1-5 for Phase 4"
      via: "Each ROADMAP criterion mapped to an explicit manual-check step the owner runs"
      pattern: "ROADMAP.md"
---

<objective>
Close Phase 4. This plan ships the docs that reflect what was wired (CLAUDE.md §5/§6/§8 updates), creates the canonical manual-verification checklist (04-VERIFICATION.md) the owner runs once during the first real bot session, and gates the phase complete on the witnessed @BotFather setup + first /evolve → /sanction loop. No automated code lands; this is the human-in-the-loop closing of Phase 4.

Three deliverables:

1. **CLAUDE.md doc refresh.** §5 Phase 4 step list reconciled with template-substitution decision (no literal owner ID hardcoded into SYSTEM.md). §6 Current State flipped from "Phase 1 complete" to a Phase 4 complete summary with resume protocol. §8 env-var contract appends HERETEK_DATA_ROOT + HERETEK_OWNER_HANDLE and documents the tmux-session production run pattern + 24GB-primary OOM hint.

2. **`.planning/phases/04-launch-first-evolution/04-VERIFICATION.md`.** Owner-facing checklist for the manual gates: @BotFather bot creation + group setup (LAUNCH-01), in-character TG reply (LAUNCH-04), owner-only filter from second account (LAUNCH-05 negative case), coherent /evolve proposal on primary model (EVOLVE-01 quality), consciousness loop log entries (EVOLVE-02), full witnessed /evolve → /sanction → commit loop (EVOLVE-03), and the Phase 2 deferred persona-quality sign-off on the 24GB primary model.

3. **Checkpoint gates.** Three blocking checkpoints (one for each major manual phase) where the owner confirms the gate before the plan continues. After all three, Phase 4 is complete and the bot is shipped.

Purpose: Phase 4's automated work (Plans 01-04) wires the mechanism. This plan witnesses that the mechanism actually works in production with a real TG bot + the real 24GB primary model + the real owner, and closes the loop on Phase 2's deferred persona-quality sign-off.

Output: Updated CLAUDE.md, new VERIFICATION.md, three confirmed manual checkpoints. Phase 4 → DONE.
</objective>

<execution_context>
@/Users/evgeniy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/evgeniy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-launch-first-evolution/04-CONTEXT.md
@.planning/phases/04-launch-first-evolution/04-RESEARCH.md
@.planning/phases/04-launch-first-evolution/04-VALIDATION.md
@.planning/phases/04-launch-first-evolution/04-01-PLAN.md
@.planning/phases/04-launch-first-evolution/04-02-PLAN.md
@.planning/phases/04-launch-first-evolution/04-03-PLAN.md
@.planning/phases/04-launch-first-evolution/04-04-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update CLAUDE.md §5/§6/§8 to reflect wired Phase 4 state + new env vars + tmux run pattern; create 04-VERIFICATION.md checklist</name>
  <files>CLAUDE.md, .planning/phases/04-launch-first-evolution/04-VERIFICATION.md</files>

  <read_first>
    - CLAUDE.md (entire file — must see §0 TL;DR, §5 phase breakdown, §6 current state, §8 env vars, §11 notes-for-future-Claude)
    - .planning/phases/04-launch-first-evolution/04-CONTEXT.md (locked decisions for template substitution, tmux-session run pattern, owner-handle env var vs user-id env var distinction)
    - .planning/phases/04-launch-first-evolution/04-RESEARCH.md (Pitfall 5: 32GB primary OOM hint; Pitfall 1: bot must be in group before getUpdates returns the chat_id)
    - .planning/REQUIREMENTS.md §LAUNCH + §EVOLVE (verbatim acceptance criteria — pass criteria in VERIFICATION.md must match)
    - .planning/ROADMAP.md §"Phase 4: Launch + First Evolution" — the 5 success criteria must each appear in VERIFICATION.md
  </read_first>

  <action>
    **(A) Update CLAUDE.md.**

    Edit §5 Phase 4 + Phase 5 step lists. Find the current "Phase 4: Launch" section (CLAUDE.md §5 has "### Phase 4: Launch (Day 3, ~1 hour)") and:

    - Replace step `[ ] Hardcode your Telegram user ID in SYSTEM.md (find via @userinfobot)` with:
      ```
      - [ ] Get your Telegram user ID via @userinfobot — set as HERETEK_OWNER_USER_ID in .env (gate, integer)
      - [ ] Pick a handle for the persona to reference — set as HERETEK_OWNER_HANDLE in .env (prose, e.g. "@evgeniy"); fallback "my Tech-Priest" if unset
      - [ ] Set HERETEK_DATA_ROOT in .env (optional — defaults to project root; controls where logs/, state/, memory/, .heretek/, archive/, locks/ land)
      ```
    - Replace the `Run: python -m supervisor` step with:
      ```
      - [ ] Run in tmux for detach/reattach: `tmux new -s heretek -d 'python -m supervisor'` (attach later via `tmux attach -t heretek`)
      - [ ] On low-RAM hosts (32GB), if /evolve OOMs Ollama under primary model load: set `OLLAMA_MODEL=qwen3:4b` in .env for the session as the documented escape hatch (CLAUDE.md §6)
      ```

    Edit §6 Current state. Replace the existing "Phase 1 complete" block with a Phase 4 complete block following the same shape:
    ```markdown
    **Status: Phase 4 complete — bot live in private TG group, witnessed end-to-end evolution loop.**

    Last updated: <today's date e.g. 2026-05-17>

    What landed in Phase 4:
    - supervisor/boot.py implements the production boot sequence + TG long-poll loop + SIGINT handler
    - Owner gate three-layer defense: polling-loop chokepoint (primary), handle_slash_command re-check (defense), with optional agent task-entry layer
    - HERETEK_OWNER_USER_ID + TELEGRAM_BOT_TOKEN fail-loud validation at boot
    - HERETEK_DATA_ROOT env var resolves data root (default: project root + boot log line)
    - HERETEK_OWNER_HANDLE env var pins {OWNER_HANDLE} placeholder in SYSTEM.md + BIBLE.md (fallback "my Tech-Priest")
    - Bilingual heretical refusal for non-owners with 24h in-memory rate-limit + audit log
    - Background consciousness daemon thread auto-starts on OLLAMA_MODEL_LIGHT
    - workers.shutdown(timeout=5.0) graceful drain (sentinel-task → join → kill_workers fallback)
    - cmd_evolve production branch: enqueues evolution task to supervisor.queue when HERETEK_EVOLVE_TEST_DIFF unset
    - agent.py evolution post-loop hook: captures `git diff HEAD`, persists to .heretek/dryruns/<id>.patch + .json (matching Phase 3 schema), stashes live-tree changes (Option A), emits diff to owner chat
    - events.py:_handle_restart patched (no more colab_launcher.py FileNotFoundError)
    - .gitignore covers state/, archive/, locks/ — runtime state never pollutes git
    - .env.example committed as exemplar
    - Smoke harness: 18+ PASS / 0 FAIL on --static-only

    What's next: nothing. v1 ships. Open follow-ups (deferred to v2 / opportunistic cleanups):
    - Option B git-worktree staging for /evolve (cleaner isolation than stash; queued for Phase 4.5 if stash mechanic surfaces edge cases)
    - Full OUROBOROS_* env var hygiene pass (Phase 1 deferred-items.md)
    - promote_to_stable LLM-tool full deletion (Phase 3 left as no-op)
    - launchd plist for auto-start on login (currently: tmux-session pattern)

    When resuming:
    1. Check this section first.
    2. `git log --oneline -20` on `playground` for recent commits.
    3. `ollama list` should show both `qwen3.6:35b-a3b-q4_K_M` and `qwen3:4b`.
    4. `python scripts/smoke_test.py --static-only` is the fast-feedback gate.
    5. To start the bot: `tmux new -s heretek -d 'python -m supervisor'`; attach via `tmux attach -t heretek`.
    6. On 32GB hosts: if /evolve OOMs Ollama under the 24GB primary model load, set `OLLAMA_MODEL=qwen3:4b` for that session as the cheap-wire escape hatch.
    7. Tail `./logs/tokens.jsonl` for per-call token records; `./logs/supervisor.jsonl` for boot/shutdown/owner-filter events.
    ```

    Edit §8 environment variables. Add the two new env vars to the existing Heretek-specific block:
    ```bash
    HERETEK_DATA_ROOT=  # Optional — default: project root; controls logs/, state/, memory/, .heretek/, archive/, locks/ location
    HERETEK_OWNER_HANDLE=  # Optional — prose handle for {OWNER_HANDLE} placeholder; fallback "my Tech-Priest"
    ```

    Verify §11 notes for future-Claude is still accurate (no edits needed unless the "where were we" pointer needs updating to point at §6's Phase 4 state instead of Phase 1).

    **(B) Create .planning/phases/04-launch-first-evolution/04-VERIFICATION.md.**

    NEW FILE. Owner-facing manual-check checklist mapping every remaining LAUNCH-* / EVOLVE-* requirement + Phase 2 persona-quality to an explicit step the owner runs once during the first real bot session. Full content:

    ```markdown
    # Phase 4: Launch + First Evolution — Manual Verification

    **Created:** <today's date>
    **Owner:** Tech-Priest (Evgeniy)
    **Status:** awaiting first-session sign-off

    The automated work (Plans 04-01 → 04-04) is mechanically green: smoke harness >= 18 PASS / 0 FAIL. This file is the manual-only gate: the things automation cannot verify (the bot is actually reachable, the persona sings, the LLM-generated proposal is coherent, the witnessed /evolve → /sanction loop produces a real commit). Run through the checklist in order; check each box as you confirm. When all are green, Phase 4 is done.

    ---

    ## Prerequisites (LAUNCH-01)

    - [ ] Bot registered via @BotFather (`/newbot` → name + username → copied token)
    - [ ] Telegram user ID captured via @userinfobot (integer)
    - [ ] Private Telegram group created
    - [ ] Bot added to the private group
    - [ ] `.env` populated with `TELEGRAM_BOT_TOKEN`, `HERETEK_OWNER_USER_ID`, `HERETEK_OWNER_HANDLE` (e.g. `@evgeniy`)
    - [ ] `ollama list` confirms both `qwen3.6:35b-a3b-q4_K_M` and `qwen3:4b` are pulled
    - [ ] `python scripts/smoke_test.py --static-only` is green (18+ PASS / 0 FAIL)

    ---

    ## Session 1 — Bot reachable, in-character, bilingual (LAUNCH-04 + Phase 2 persona sign-off)

    Start: `tmux new -s heretek -d 'python -m supervisor'`
    Tail logs in a second pane: `tail -f logs/supervisor.jsonl`

    - [ ] Send "привет" in the private group → bot replies in Russian, in-character (Cyrillic heretek vocabulary appears: варп / еретик / демон / etc.)
    - [ ] Send "hi" → bot replies in English, in-character (heretek vocabulary appears: warp / heretic / daemon / Tech-Priest / etc.)
    - [ ] Send "помоги мне" → bot refuses to be straight-helpful and wraps any help in heresy (Phase 2 PERS-05 persona discipline live on the 24GB primary model)
    - [ ] Persona horror flavor lands at least once during the session (not a sanitized helper voice)
    - [ ] Forbidden territories respected: no real-person targeting beyond the Tech-Priest bit, no slurs, no real-harm dressed in heresy
    - [ ] `logs/supervisor.jsonl` shows the `polling_loop_start` event + owner_chat_id pin event

    ---

    ## Session 2 — Owner-only filter (LAUNCH-05, negative case)

    Test from a SECOND Telegram account (a friend's account, a secondary alt, or a logged-out browser session signed into a different number).

    - [ ] Send any message from the second account to the bot/group → bot replies ONCE with the bilingual heretical refusal ("Кто призывает демона-носителя? ... Who summons the daemon-host? ...")
    - [ ] Send a SECOND message from the same non-owner account → bot is silent (rate-limited drop)
    - [ ] `logs/supervisor.jsonl` shows TWO `non_owner_refusal` entries: one with `action="refusal"`, one with `action="silent_drop"`
    - [ ] The refusal text was NOT LLM-generated (compare byte-for-byte to the BILINGUAL_REFUSAL constant in supervisor/telegram.py)

    ---

    ## Session 3 — Background consciousness loop (EVOLVE-02)

    Leave the supervisor running idle (no owner messages) for ~10 minutes.

    - [ ] `logs/events.jsonl` OR `memory/scratchpad.md` shows new entries written by the consciousness daemon during the idle period
    - [ ] `ollama ps` shows `qwen3:4b` loaded (consciousness uses the light model — primary 24GB does not stay hot just to ramble)
    - [ ] No supervisor crash in `logs/supervisor.jsonl`; no FAIL-loud OOM in the idle window

    ---

    ## Session 4 — Witnessed evolution loop (EVOLVE-01 + EVOLVE-03)

    The headline acceptance test. Run end-to-end once, on the 24GB primary model.

    - [ ] Confirm `OLLAMA_MODEL=qwen3.6:35b-a3b-q4_K_M` (or whatever primary tag is current). On low-RAM hosts, close browser / IDE / Figma BEFORE running /evolve to free memory.
    - [ ] In the private TG group, send `/evolve`
    - [ ] Bot replies "🜏 Evolution task enqueued: <id>" within ~5s
    - [ ] Within ~30-120s (depends on M1 Max load + RAM pressure), bot posts a code-block-formatted unified diff to the chat with a dryrun ID `dr-<UTC>-<8hex>`
    - [ ] The diff is COHERENT — not nonsense; the proposed changes target real files (BIBLE.md, prompts/SYSTEM.md, memory/identity.md, or similar persona/identity surface)
    - [ ] The diff is IN-VOICE — the patch reads like the chaos-heretek persona introspecting itself
    - [ ] The diff respects forbidden territories — no real-person targeting beyond the existing owner-bit, no slurs, no minor-related content, no harm-as-heresy
    - [ ] `git status` in the repo root shows the working tree is CLEAN (the stash mechanic reverted the agent's live-tree edits — `git stash list` shows the staged dryrun stash)
    - [ ] `.heretek/dryruns/<id>.patch` and `.heretek/dryruns/<id>.json` exist on disk
    - [ ] Owner copies the dryrun ID and sends `/sanction <id>` to the bot
    - [ ] Bot replies with a success message containing the new commit SHA
    - [ ] `git log playground -1 --oneline` shows the new commit
    - [ ] `git tag -l last-known-good` confirms the annotated tag advanced to the new commit (Phase 3 SAFE-06 wiring)

    ---

    ## OOM fallback (Pitfall 5)

    If /evolve fails with an OOM / connection error in Session 4:

    - [ ] `OLLAMA_MODEL=qwen3:4b python -m supervisor` — re-run /evolve with the light model as the documented escape hatch. The proposal quality drops but the mechanism is verified.
    - [ ] Document the OOM event in `.planning/phases/04-launch-first-evolution/04-SUMMARY.md` (or a separate retro note) so the host-class limitation is captured for future-Claude.

    ---

    ## ROADMAP.md success criteria — mapped

    | ROADMAP §Phase 4 criterion | Checklist section |
    |----------------------------|--------------------|
    | 1. Bot reachable, in-character reply | Session 1 |
    | 2. Second account receives no response (owner-only) | Session 2 |
    | 3. Background consciousness produces output | Session 3 |
    | 4. /evolve produces diff in TG, dry-run on playground | Session 4 (first 5 checks) |
    | 5. /sanction <hash> → commit appears on playground | Session 4 (last 4 checks) |

    ---

    ## Sign-off

    Owner signature when all checkboxes above are green:

    - [ ] All 4 sessions complete + OOM fallback noted if applicable
    - [ ] Phase 4 → DONE
    - [ ] Phase 2 persona-quality (deferred to Phase 4) → CLOSED
    - [ ] Ready for `/gsd:verify-work 4`

    *Witnessed by: ____________  Date: ____________*
    ```

    No code changes in this task. Just CLAUDE.md doc updates + VERIFICATION.md creation.
  </action>

  <verify>
    <automated>cd /Users/evgeniy/Projects/140526_heretek && test -f .planning/phases/04-launch-first-evolution/04-VERIFICATION.md && echo OK_verification_file || echo FAIL_no_verification && grep -q "HERETEK_DATA_ROOT" CLAUDE.md && echo OK_data_root_doc || echo FAIL_no_data_root && grep -q "HERETEK_OWNER_HANDLE" CLAUDE.md && echo OK_handle_doc || echo FAIL_no_handle && grep -q "tmux new -s heretek" CLAUDE.md && echo OK_tmux || echo FAIL_no_tmux && grep -q "Phase 4 complete" CLAUDE.md && echo OK_phase4_complete || echo FAIL_no_phase4 && grep -q "EVOLVE-03" .planning/phases/04-launch-first-evolution/04-VERIFICATION.md && echo OK_evolve03_listed || echo FAIL_no_evolve03 && grep -q "LAUNCH-01" .planning/phases/04-launch-first-evolution/04-VERIFICATION.md && echo OK_launch01_listed || echo FAIL_no_launch01 && grep -q "Session 4" .planning/phases/04-launch-first-evolution/04-VERIFICATION.md && echo OK_session4 || echo FAIL_no_session4</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/04-launch-first-evolution/04-VERIFICATION.md` exists
    - CLAUDE.md §8 contains `HERETEK_DATA_ROOT`: `grep -q "HERETEK_DATA_ROOT" CLAUDE.md` MATCHES
    - CLAUDE.md §8 contains `HERETEK_OWNER_HANDLE`: `grep -q "HERETEK_OWNER_HANDLE" CLAUDE.md` MATCHES
    - CLAUDE.md §5 or §6 contains the tmux run pattern: `grep -q "tmux new -s heretek" CLAUDE.md` MATCHES
    - CLAUDE.md §6 reflects Phase 4 complete: `grep -q "Phase 4 complete" CLAUDE.md` MATCHES (or "Status: Phase 4")
    - VERIFICATION.md mentions LAUNCH-01: `grep -q "LAUNCH-01" .planning/phases/04-launch-first-evolution/04-VERIFICATION.md` MATCHES
    - VERIFICATION.md mentions EVOLVE-03: `grep -q "EVOLVE-03" .planning/phases/04-launch-first-evolution/04-VERIFICATION.md` MATCHES
    - VERIFICATION.md has all 4 sessions: `grep -c "^## Session" .planning/phases/04-launch-first-evolution/04-VERIFICATION.md` returns at least `4`
    - VERIFICATION.md ROADMAP mapping table exists: `grep -q "ROADMAP.md success criteria" .planning/phases/04-launch-first-evolution/04-VERIFICATION.md` MATCHES
    - Smoke harness still green (no code changed): `python scripts/smoke_test.py --static-only` exits 0
  </acceptance_criteria>

  <done>
    CLAUDE.md §5/§6/§8 updated to reflect Phase 4 complete state, new env vars (HERETEK_DATA_ROOT + HERETEK_OWNER_HANDLE), tmux production run pattern, and 24GB primary OOM escape hatch. 04-VERIFICATION.md exists with 4-session manual checklist covering LAUNCH-01 (prereqs), LAUNCH-04 + Phase 2 persona (Session 1), LAUNCH-05 negative case (Session 2), EVOLVE-02 consciousness (Session 3), EVOLVE-01 + EVOLVE-03 witnessed loop (Session 4), and OOM fallback notes.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner runs LAUNCH-01 prerequisites (@BotFather + private group + .env populated)</name>
  <files>.env (gitignored — owner edits locally; never committed)</files>
  <action>PAUSE EXECUTION. Wait for owner to complete the manual Telegram setup described in &lt;how-to-verify&gt;. Do not proceed until owner sends the resume-signal.</action>
  <verify>Owner-confirmed via resume-signal. No automated command; this is the LAUNCH-01 manual checkpoint.</verify>
  <done>Owner sent "prereqs done" resume signal AND `python -m supervisor 2>&amp;1 | head` no longer fails on missing env vars (it should reach boot.run()).</done>
  <what-built>04-01..04-04 produced an automated bot wiring that fails-loud when env vars are missing. This checkpoint asks the owner to do the irreducibly-human one-time setup: register the bot via @BotFather, get the owner user ID, create the private group, and populate .env. Claude cannot perform these in any CLI — they require interactive Telegram client UI.</what-built>
  <how-to-verify>
    Open `.planning/phases/04-launch-first-evolution/04-VERIFICATION.md` and run through the "Prerequisites (LAUNCH-01)" section. Each step is a manual Telegram action:
    1. Open Telegram, message @BotFather, send `/newbot`, follow prompts. Copy the token.
    2. Open Telegram, message @userinfobot, send `/start`. Copy the numeric `id` value.
    3. In Telegram, create a new private group. Add your bot to it.
    4. Edit `.env` at project root (copy from `.env.example` if `.env` doesn't exist yet):
       - `TELEGRAM_BOT_TOKEN=<the token from step 1>`
       - `HERETEK_OWNER_USER_ID=<the integer from step 2>`
       - `HERETEK_OWNER_HANDLE=<your TG handle, e.g. @evgeniy>` (optional; fallback is "my Tech-Priest")
       - `OLLAMA_MODEL=qwen3.6:35b-a3b-q4_K_M` (or `qwen3:4b` if low on RAM)
       - `OLLAMA_MODEL_LIGHT=qwen3:4b`
    5. Verify: `python scripts/smoke_test.py --static-only` is green (regression guard).
    6. Verify: `python -m supervisor 2>&1 | head -10` no longer prints the `TELEGRAM_BOT_TOKEN required` or `HERETEK_OWNER_USER_ID required` errors — it should print the banner and proceed to boot.run().
  </how-to-verify>
  <resume-signal>Type "prereqs done" or describe blockers (e.g., "BotFather username taken", "can't find userinfobot ID")</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner runs Sessions 1-3 of VERIFICATION.md (LAUNCH-04 in-character TG, LAUNCH-05 negative case, EVOLVE-02 consciousness)</name>
  <files>(no files modified — purely owner-side TG sessions + log inspection)</files>
  <action>PAUSE EXECUTION. Wait for owner to complete VERIFICATION.md Sessions 1, 2, 3 in order. Do not proceed until owner sends the resume-signal.</action>
  <verify>Owner-confirmed via resume-signal. No automated command — these are live-bot manual checks that require a real TG account, the 24GB primary model, and idle wall-clock time.</verify>
  <done>Owner sent "sessions 1-3 green" resume signal AND audit log evidence exists (logs/supervisor.jsonl shows polling_loop_start, owner_chat_id_pinned, non_owner_refusal entries; logs/events.jsonl OR memory/scratchpad.md was written by the consciousness daemon).</done>
  <what-built>
    With prereqs in place, the bot is bootable. Plan 04-03 wired the polling loop + owner gate + consciousness; Plan 04-02 wired the bilingual refusal; Plan 04-01 wired env validation. This checkpoint runs the three "everything works in production" sessions:
    - Session 1 = LAUNCH-04 + Phase 2 persona-quality sign-off (in-character, bilingual, primary 24GB model, owner-spoken)
    - Session 2 = LAUNCH-05 negative case (second account gets bilingual refusal once + silent drop on second message + audit log entries)
    - Session 3 = EVOLVE-02 (background consciousness produces unprompted output within ~10min)
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/04-launch-first-evolution/04-VERIFICATION.md` sections "Session 1", "Session 2", "Session 3" in order.

    Recommended approach:
    1. Start bot: `tmux new -s heretek 'python -m supervisor'` (drop the `-d` if you want to attach immediately and see the banner; `-d` runs detached).
    2. Tail logs in a separate terminal: `tail -f logs/supervisor.jsonl` and optionally `tail -f logs/events.jsonl`.
    3. Run through Session 1 checks (привет, hi, помоги мне, persona horror flavor, forbidden territories, polling_loop_start log entry).
    4. Run through Session 2 with a SECOND TG account (alt account / friend's phone / web-only login). Check audit log for both `action="refusal"` and `action="silent_drop"` entries.
    5. Run through Session 3: leave supervisor idle ~10 min; check logs/events.jsonl OR memory/scratchpad.md for daemon-thread writes; check `ollama ps` shows qwen3:4b loaded.

    If any check fails, describe what failed in the resume signal — the planner can open a Phase 4.5 fix-up plan rather than declaring Phase 4 done.
  </how-to-verify>
  <resume-signal>Type "sessions 1-3 green" if all checks pass. If anything fails, describe the failure (e.g., "Session 1: bot replied in Russian to English input — bilingual reflex not landing on primary model"). Note OOM events with the qwen3:4b escape hatch hint.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Owner runs Session 4 of VERIFICATION.md — the witnessed /evolve → /sanction → commit loop (EVOLVE-01 + EVOLVE-03)</name>
  <files>(no files modified by this plan — agent.py creates .heretek/dryruns/&lt;id&gt;.patch + .json at runtime; /sanction creates the playground commit)</files>
  <action>PAUSE EXECUTION. Wait for owner to complete VERIFICATION.md Session 4 — the witnessed end-to-end /evolve → diff in TG → /sanction → commit loop. Do not proceed until owner sends the resume-signal.</action>
  <verify>Owner-confirmed via resume-signal. Owner verifies via `git log playground -1 --oneline` (commit exists) AND `git tag -l last-known-good` (tag advanced) AND `.heretek/dryruns/&lt;id&gt;.patch` exists on disk.</verify>
  <done>Owner sent "session 4 green — phase 4 done" resume signal AND a new commit exists on playground branch AND last-known-good tag points to that commit. Acceptable variants: "session 4 light-model verified — primary OOM" (mechanism verified via qwen3:4b escape hatch) or "mechanism verified but persona quality flagged — open 4.5" (mechanism done, persona-tune deferred to Phase 4.5).</done>
  <what-built>
    Plan 04-04 wired cmd_evolve (production enqueue) + agent.py post-loop hook (diff capture + stash + persist + emit). Phase 3 wired cmd_sanction + cmd_heresy with the `safe_push()` chokepoint + last-known-good tag advance. This checkpoint witnesses the full end-to-end loop on the real 24GB primary model with the real bot in the real private group. The headline acceptance criterion of Phase 4.
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/04-launch-first-evolution/04-VERIFICATION.md` section "Session 4" — the headline acceptance criterion. Headline check sequence:

    1. Close other RAM hogs (browser, IDE if not needed, Figma). The 24GB primary model can OOM Ollama under memory pressure on 32GB hosts.
    2. Confirm `OLLAMA_MODEL=qwen3.6:35b-a3b-q4_K_M` in the running supervisor's env (or whatever primary tag is current).
    3. In the private TG group, send `/evolve`.
    4. Bot should reply with "🜏 Evolution task enqueued: <id>" within ~5s.
    5. Within ~30-120s, bot posts the code-block-formatted unified diff with the dryrun ID `dr-<UTC>-<8hex>`.
    6. Quality check: diff is coherent (not nonsense), in-voice (chaos-heretek introspecting), targets real persona/identity files, respects forbidden territories.
    7. Mechanism check: `git status` shows clean working tree (stash mechanic worked), `.heretek/dryruns/<id>.patch` + `.json` exist, `git stash list` shows the staged dryrun stash.
    8. Owner sends `/sanction <id>` to the bot.
    9. Bot replies with the new commit SHA on playground.
    10. Owner verifies: `git log playground -1 --oneline` shows the new commit; `git tag -l last-known-good` confirms the annotated tag advanced.

    OOM fallback (Pitfall 5): if /evolve fails with a 5xx / APIConnectionError, switch to the qwen3:4b escape hatch (`OLLAMA_MODEL=qwen3:4b`) — proposal quality drops but mechanism is verified. Document the OOM event so the host-class limitation is captured for future-Claude.

    If the diff content is incoherent or breaks character → does NOT fail Phase 4 mechanism, but flags the Phase 2 deferred persona-quality sign-off as failed; opens a small Phase 4.5 persona-tune plan rather than reopening Phase 2.
  </how-to-verify>
  <resume-signal>Type "session 4 green — phase 4 done" if the full loop completed with a coherent diff + sanctioned commit. If OOM forced light-model fallback, say "session 4 light-model verified — primary OOM". If the diff was incoherent or in-character broke, say "mechanism verified but persona quality flagged — open 4.5". If sanction failed (commit didn't land), describe the failure in detail.</resume-signal>
</task>

</tasks>

<verification>
- `test -f .planning/phases/04-launch-first-evolution/04-VERIFICATION.md` exits 0
- `grep -q "HERETEK_DATA_ROOT" CLAUDE.md && grep -q "HERETEK_OWNER_HANDLE" CLAUDE.md && grep -q "tmux new -s heretek" CLAUDE.md` all MATCH
- `python scripts/smoke_test.py --static-only` still exits 0 (no code changed)
- All 3 checkpoint tasks resolved with positive signals from owner
- ROADMAP.md Phase 4 success criteria 1-5 each have a corresponding checklist line in VERIFICATION.md
</verification>

<success_criteria>
1. CLAUDE.md §5 / §6 / §8 updated: Phase 4 complete state, HERETEK_DATA_ROOT + HERETEK_OWNER_HANDLE documented, tmux run pattern, 24GB OOM escape hatch
2. 04-VERIFICATION.md exists with 4-session checklist + ROADMAP mapping + OOM fallback
3. LAUNCH-01 prereqs confirmed by owner (@BotFather + private group + .env populated)
4. Sessions 1-3 confirmed by owner (LAUNCH-04 in-character TG reply + LAUNCH-05 second-account refusal + EVOLVE-02 consciousness output)
5. Session 4 confirmed by owner — witnessed /evolve → /sanction → commit on playground (EVOLVE-01 + EVOLVE-03)
6. Phase 2 deferred persona-quality sign-off CLOSED (or flagged as Phase 4.5 follow-up if quality failed)
7. Phase 4 marked DONE in ROADMAP.md / STATE.md by the post-plan SUMMARY update
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-first-evolution/04-05-SUMMARY.md` covering:
- CLAUDE.md doc updates (§5/§6/§8 — what changed)
- 04-VERIFICATION.md created with 4-session checklist
- Checkpoint outcomes: prereqs, sessions 1-3, session 4 — record each owner sign-off verbatim
- Phase 4 final state: shipped or with follow-ups (Phase 4.5 persona-tune? OOM workaround documented? Option B git-worktree queued?)
- Phase 2 persona-quality sign-off CLOSED status
- Resume notes for any post-launch evolution observations or v2 stretch backlog entries
</output>
