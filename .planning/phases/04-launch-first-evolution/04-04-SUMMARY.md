---
phase: 04-launch-first-evolution
plan: "04"
subsystem: evolve-production-wiring
tags: [evolve, queue, agent, dryrun, git-stash, supervisor, telegram]

dependency_graph:
  requires:
    - 04-01: env-validation-scaffold + SKIP-stub test_evolve_enqueues_task_when_no_fixture
    - 04-02: is_owner_message + BILINGUAL_REFUSAL + Layer 2 slash guard
    - 04-03: boot.py polling loop + owner_chat_id persisted to state.json + queue infra live
    - 03-03: Phase 3 cmd_sanction + .heretek/dryruns schema (dr-<UTC>-<8hex>) + fixture seam
  provides:
    - supervisor/commands.py:cmd_evolve production branch: enqueues {type:'evolution', source:'/evolve', chat_id:owner_chat_id, text:seed} into supervisor.queue.PENDING when HERETEK_EVOLVE_TEST_DIFF unset
    - supervisor/commands.py:cmd_evolve: friendly error when owner_chat_id unset; fixture path unchanged for smoke tests
    - heretek/agent.py:_capture_evolution_dryrun(): post-loop hook captures git diff HEAD, persists dr-<UTC>-<8hex>.patch + .json, stashes live tree, emits code-block diff to owner chat_id
    - Full /evolve -> diff in TG -> /sanction <id> -> commit loop mechanically wired (EVOLVE-01 + EVOLVE-03 plumbing)
  affects:
    - 04-05: manual VERIFICATION.md session (EVOLVE-03 witnessed end-to-end loop + LAUNCH-04 persona quality)

tech-stack:
  added: []
  patterns:
    - "Option A staging: agent writes to live tree; post-loop git diff HEAD; git stash push -u to revert; diff persisted to .heretek/dryruns/"
    - "Lazy import inside function body: supervisor.queue + supervisor.state imported inside cmd_evolve production branch (Pitfall 3 mitigation)"
    - "Dryrun ID schema dr-<UTC>-<8hex> used consistently in both fixture path (cmd_evolve) and production path (_capture_evolution_dryrun); /sanction works unchanged for both"
    - "sha256 sidecar written by both paths; cmd_sanction verifies before applying (tamper detection)"

key-files:
  created: []
  modified:
    - supervisor/commands.py
    - heretek/agent.py
    - scripts/smoke_test.py

key-decisions:
  - "Option A (post-loop git stash/diff) over Option B (git worktree): fewer code changes, simpler integration; Option B noted as Phase 4.5 improvement if stash edge cases surface"
  - "Dryrun ID in production path generated by _capture_evolution_dryrun() (not cmd_evolve): agent runs and produces the actual diff; ID is stamped at capture time"
  - "OLLAMA_MODEL=qwen3:4b escape hatch documented: 24GB primary can OOM on 32GB host during /evolve; set OLLAMA_MODEL=qwen3:4b for low-RAM sessions"
  - "test_evolve_enqueues_task_when_no_fixture flipped from SKIP to live PASS in same task as production implementation (TDD pattern: RED -> GREEN)"
  - "Auto-rescue committed changes: project's auto-rescue hook intercepted file edits before manual git add; commits faf7761 (commands.py), f4eca3e (smoke_test.py), e0b9f5f (agent.py)"

patterns-established:
  - "Evolution post-loop hook: any future task type can add a post-loop hook in handle_task() guarded by task_type_str == '<type>'"
  - "Dryrun persistence: .heretek/dryruns/<id>.patch + .heretek/dryruns/<id>.json sidecar is the canonical format for all dryrun proposals (fixture and production)"

requirements-completed: [EVOLVE-01, EVOLVE-03]

duration: 4min
completed: "2026-05-17"
---

# Phase 4 Plan 04: Production /evolve Wiring Summary

**Production /evolve wired end-to-end: cmd_evolve enqueues evolution tasks; agent.py post-loop hook captures git diff HEAD, stashes live tree, persists to .heretek/dryruns/, emits diff to owner chat as dry-run proposal**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-17T07:13:43Z
- **Completed:** 2026-05-17T07:18:17Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- `supervisor/commands.py:cmd_evolve` now branches on `HERETEK_EVOLVE_TEST_DIFF`: env-unset -> production enqueue path; env-set -> Phase 3 fixture path (unchanged). Friendly error when `owner_chat_id` unset.
- `heretek/agent.py:_capture_evolution_dryrun()` post-loop hook: captures `git diff HEAD`, persists `dr-<UTC>-<8hex>.patch + .json` (matching Phase 3 `/sanction` schema), stashes live-tree changes via `git stash push -u`, emits code-block-formatted diff to owner's `chat_id` via `send_with_budget`.
- Smoke harness: `test_evolve_enqueues_task_when_no_fixture` flipped from SKIP to live PASS (18 PASS / 0 FAIL / 1 SKIP). Phase 3 fixture tests all still PASS.

## Task Commits

All changes auto-rescued by project's startup hook (committed before this executor's git add):

1. **Task 1: cmd_evolve production branch + smoke test flip** — `f4eca3e` (scripts/smoke_test.py), `faf7761` (supervisor/commands.py)
2. **Task 2: agent.py evolution post-loop hook** — `e0b9f5f` (heretek/agent.py)

**Plan metadata:** (this commit)

## Files Created/Modified

- `supervisor/commands.py` — Added production branch in `cmd_evolve()`: lazy-imports `supervisor.queue` and `supervisor.state`, reads `owner_chat_id` from state, enqueues `{type:'evolution', source:'/evolve', chat_id, text:seed_text}` task; preserves entire Phase 3 fixture path verbatim
- `heretek/agent.py` — Added `_capture_evolution_dryrun()` method to `OuroborosAgent`; added post-loop call block guarded by `task_type_str == "evolution"` after `run_llm_loop` returns
- `scripts/smoke_test.py` — `test_evolve_enqueues_task_when_no_fixture` replaced from SKIP stub to 3-case live test (no-owner-error, enqueue-confirmed, fixture-path-still-works)

## Decisions Made

- **Option A staging (live-tree + post-loop stash)**: simpler than Option B (git worktree); no need to plumb staging_dir through Env + ToolContext. Option B remains a Phase 4.5 improvement if edge cases (untracked file conflicts, stash collisions) surface.
- **Dryrun ID generated at capture time** (in `_capture_evolution_dryrun`), not at enqueue time in `cmd_evolve`. The evolution loop may fail or produce no diff; stamping the ID at capture is accurate.
- **OLLAMA_MODEL=qwen3:4b escape hatch**: documented in SUMMARY. The 24GB primary model can OOM Ollama on the 32GB M1 Max host. For low-RAM sessions: `OLLAMA_MODEL=qwen3:4b python -m supervisor`.
- **sha256 sidecar matches Phase 3**: `cmd_sanction` verifies the sha256 before applying; `_capture_evolution_dryrun` writes the same sidecar shape. /sanction works on production dryruns unchanged.

## Deviations from Plan

### Auto-fixed Issues

None - plan executed exactly as written.

### Process Deviation: Auto-rescue commits

The project's auto-rescue startup hook intercepted uncommitted file changes and created commits before this executor's `git add` calls:
- `f4eca3e` — scripts/smoke_test.py (live test body written before implementation as TDD RED)
- `faf7761` — supervisor/commands.py (production branch implementation)
- `e0b9f5f` — heretek/agent.py (post-loop hook)

This is expected project behavior (not a bug). All changes are correctly committed; commit content verified via `git show`.

## Issues Encountered

None — all code paths verified cleanly on first attempt.

## Open Follow-ups

- **Option B (git worktree staging)**: if `git stash push -u` surfaces edge cases (untracked file conflicts, stash count accumulation), switch evolution tasks to a proper git worktree at `.heretek/staging/`. Deferred to Phase 4.5.
- **OOM hint in /evolve error path**: if Ollama returns a 5xx during the evolution agent loop, the error surfaces as `⚠️ Ошибка при обработке` in `_emit_task_results`. A dedicated OOM-detection wrapper could add the `OLLAMA_MODEL=qwen3:4b` hint. Deferred — the CLAUDE.md §6 documentation is the primary mitigation.
- **EVOLVE-03 manual sign-off**: the full `/evolve → diff in TG → /sanction <id> → commit on playground` loop is mechanically wired but has NOT been run with a live LLM. Plan 04-05's VERIFICATION.md is the witnessed-evolution gate.

## Next Phase Readiness

Plan 04-05: documentation + manual verification checklist:
- LAUNCH-01: @BotFather bot registration + private group setup (manual)
- LAUNCH-04: first in-character TG session (manual persona quality sign-off)
- EVOLVE-01: quality of LLM-generated diff proposal (witnessed on primary model)
- EVOLVE-03: full `/evolve → /sanction` loop in the private TG group

The bot is now fully mechanically wired for the first witnessed evolution session. The remaining gate is environmental: `TELEGRAM_BOT_TOKEN` + `HERETEK_OWNER_USER_ID` set, Ollama primary model loaded, `python -m supervisor` running in a tmux session.

## Self-Check: PASSED

Files verified:
- `supervisor/commands.py` — FOUND, contains "Evolution task enqueued", "owner_chat_id", "Tech-Priest demands your next mutation"
- `heretek/agent.py` — FOUND, contains "_capture_evolution_dryrun", "git diff HEAD", "git.*stash.*push", ".heretek/dryruns", "dr-", "send_with_budget", `task_type_str == "evolution"`
- `scripts/smoke_test.py` — FOUND, test_evolve_enqueues_task_when_no_fixture is live PASS

Smoke gate: 18 pass · 0 fail · 1 skip · 19 total

Commits verified in git log:
- `f4eca3e` — FOUND (smoke_test.py live test)
- `faf7761` — FOUND (commands.py production branch)
- `e0b9f5f` — FOUND (agent.py post-loop hook)
